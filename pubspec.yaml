name: sirbituu
description: AI Music Studio Afaan Oromoof
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # iOS style icons
  cupertino_icons: ^1.0.2

  # State management
  provider: ^6.0.5

  # Audio playback
  audioplayers: ^5.2.1

  # Audio recording (REPLACED audio_recorder COMPLETELY)
  record: ^5.0.4

  # Share audio files
  share_plus: ^7.2.1

  # Permissions (microphone, storage)
  permission_handler: ^11.3.0

  # File paths
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets (empty for now – no problem)
  assets:
    - assets/
